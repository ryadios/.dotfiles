#!/bin/bash

TIMESTAMP=$(date +'%Y%m%d-%H%M%S')
DIR=~/Pictures/Screenshots
FILE="$DIR/ss-$TIMESTAMP.png"

if [[ "$1" == "area" ]]; then
    grim -g "$(slurp)" "$FILE"
else
    grim "$FILE"
fi

wl-copy < "$FILE"
notify-send -u low -i "$FILE" "Screenshot Captured"
